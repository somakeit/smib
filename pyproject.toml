[tool.poetry]
name = "smib"
version = "0.1.0"
description = ""
authors = ["Sam Cork <sam57719@gmail.com>"]

packages = [
    { include = "smib", from = "src" }
]

[tool.poetry.dependencies]
python = "^3.12"
python-decouple = "^3.8"
slack-bolt = {extras = ["asyncio"], version = "^1.20.1"}


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
